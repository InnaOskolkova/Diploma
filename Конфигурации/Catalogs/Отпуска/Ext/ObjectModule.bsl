Процедура ПередЗаписью(Отказ)
	День = ДатаНачалаОтпуска;
	Пока Не День > ДатаКонцаОтпуска Цикл
		Запись = РегистрыСведений.ОтпускаСотрудников.СоздатьМенеджерЗаписи();
		Запись.Период = День;
		Запись.Сотрудник = Исполнитель;
		День = День + 86400;
		Запись.Записать(Истина);
	КонецЦикла;
КонецПроцедуры

